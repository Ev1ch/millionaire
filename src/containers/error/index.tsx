import React from 'react';
import { Error as ErrorPage } from 'components/pages';

function Error() {
    return <ErrorPage title="Unknown error occured" message="Try to refresh the page." />;
}

export default Error;
